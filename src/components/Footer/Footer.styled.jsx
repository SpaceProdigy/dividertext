import { Box, styled } from "@mui/material";
import { motion } from "framer-motion";

export const S_QuoteWrapper = styled(Box)`
  margin: 1rem;
`;

export const M_QuoteWrapper = motion(S_QuoteWrapper);
